<!DOCTYPE html>
<html>
<head>
<style>
html {
    min-height: 100%; /* This is required to ensure that the gradient stretches across the whole page */
}
 
body { #sky blue
    background: -webkit-linear-gradient(#33C7FF, #D2F3FF); 
    background: -o-linear-gradient(#33C7FF, #D2F3FF); 
    background: -moz-linear-gradient(#33C7FF, #D2F3FF); 
    background: linear-gradient(#33C7FF, #D2F3FF);  
    background-color: #93B874; 

}
h1 {
    text-align: center;
    font-family:Impact;
    font-size: 500%;
}
h4
{
    text-align: center;
}
p{
    font-size: 120%;
}
</style>
</head>
<body>
	<h1> Blog Posts </h1>
	<h4> <%= link_to 'New Post', new_post_path %> | <%= link_to 'New Author', new_author_path%> </h4>
    <% if @posts.size == 0 %>
    <h2> No posts stored </h2>
    <% else %>
	<% @posts.each do |post| %>
	<h2> <%=link_to post.title, post_path(post) %> </h2>
		<p> <%= post.body.truncate_words(100) %> </p>
		<h3> Author: <%= link_to post.author.first_and_last, author_path(post.author)%> <br>
            Posted at:  <%= post.created_at%> </h3> <br>
	<% end %>
    <% end %>
</body>
</html>
